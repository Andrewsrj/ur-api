export interface Post {
    author: { 
        id: string,
    }, 
    comments: number, 
    date: string, 
    distance: number, 
    duration: string, 
    likes: number, 
    mapImage: string, 
    pace: string
}